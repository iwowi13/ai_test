# 2026-05-21T13:30Z — Inara
- **Task:** Step 24 — admin create post (title, content, optional image upload).
- **Mode:** background
- **Inputs read:** `LoginComponent` (reactive-form reference shape), `PostsService.create` + `uploadImage`, Posts API contract (5 MB + `image/*`).
- **Outputs:** commit `73ae96d` — new `AdminPostCreateComponent` folder under `components/admin-post-create/`; standalone, `OnPush`, `fb.nonNullable.group({ title: required+maxLength(200), body: '' })`; client-side image validation mirrors backend (`image/*` + 5 MB) so we fail fast; submit pipes `create()` → `switchMap` to optional `uploadImage(id, file)`; `finalize` always resets `submitting`; status-aware error messages (401/413/400/generic); navigates to `/admin` on success. Route `/admin/posts/new` added behind `authGuard`. Spec covers required-validator block, happy create+navigate, create+upload happy path, non-image rejected client-side, oversize rejected client-side.
- **Outcome:** Authors can now create a post and attach an image in one flow. Mirrors the backend rules exactly so most invalid uploads never hit the wire.
