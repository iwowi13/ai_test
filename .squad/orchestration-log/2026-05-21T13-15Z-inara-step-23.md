# 2026-05-21T13:15Z — Inara
- **Task:** Step 23 — admin posts list with Edit + Delete (confirm before delete).
- **Mode:** background
- **Inputs read:** `PostsService` (`list`, `delete`), `AdminComponent` placeholder, `app.routes.ts`.
- **Outputs:** commit `764cfa4` — `AdminComponent` rewritten as a real list (signals + `OnPush`); rows show title + slug + Edit/Delete buttons; Delete uses native `confirm()` then calls `PostsService.delete(id)` and prunes the row on 204; distinguishes 403 vs generic failure into `deleteError`. Spec covers list render, empty state, confirm-yes → delete + prune, confirm-no → no-op, delete-failure path, Edit-button navigation.
- **Outcome:** Admin list works end-to-end behind `authGuard`. Native `confirm()` chosen deliberately (recorded in admin-ui decisions inbox) — accessible, zero design-system dependency, acceptable for v0.1.
