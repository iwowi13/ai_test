# 2026-05-21T13:00Z — Inara
- **Task:** Step 22 — functional `authGuard` on `/admin/**`, redirects unauthenticated users to `/login?returnUrl=<url>`.
- **Mode:** background
- **Inputs read:** `frontend/src/app/services/auth.service.ts` (`isAuthenticated` computed signal), `app.routes.ts` admin route block, `LoginComponent` (already honours `returnUrl`).
- **Outputs:** commit `f656a3c` — new `frontend/src/app/services/auth.guard.ts` (`CanActivateFn`: returns `true` when authed, else `router.createUrlTree(['/login'], { queryParams: { returnUrl: state.url } })`); `auth.guard.spec.ts` covers both branches via a writable signal stub for `AuthService.isAuthenticated`; `canActivate: [authGuard]` wired on the `/admin` route in `app.routes.ts`.
- **Outcome:** Admin route is now login-gated and round-trips back to the original URL post-login. Frontend specs green; foundation ready for the admin CRUD components in Steps 23–25.
